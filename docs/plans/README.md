# Plans

Complex work should be captured as execution plans.

- `active/`: plans currently being worked on.
- `completed/`: finished plans and decision history.

Small changes can use lightweight notes in the conversation, but architectural changes should become checked-in plan files.
